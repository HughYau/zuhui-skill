interface:
  display_name: "zuhui"
  short_description: "Draft research progress updates for advisors and labs"
  default_prompt: "Use $zuhui to draft a concise research progress update from my recent work."

policy:
  allow_implicit_invocation: true
